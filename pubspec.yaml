name: dee_responsive_layout
description: "A Flutter library for creating responsive layouts using a column-based grid system, similar to Bootstrap."
version: 0.0.1+3
homepage: "https://github.com/rkpontes/dee_responsive_layout"

environment:
  sdk: ">=3.4.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
